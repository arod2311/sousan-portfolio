# Sousan (Portfolio)

Sanitized export of a production codebase. Secrets and private data removed.

## Highlights
- Stack: Node/TypeScript, AWS integrations
- Practices: PR-only to main, CI-ready, linear history
- Files removed: `.env`, keys, DBs, node_modules, etc.

## Run (local)
```bash
npm install
npm run dev

test
